# Classification Problem

A supervised learning task where the model predicts a discrete category (e.g. fraud/not fraud) rather than a continuous value.

## Key Points

- Fraud detection is typically framed as binary classification
- Contrast with regression, which predicts continuous values
