# Logistic Regression

A simple, interpretable linear model that estimates the probability of the positive class using a weighted sum of features passed through a sigmoid function.

## Key Points

- Fast, interpretable, and a strong baseline before trying complex models
- Struggles with non-linear feature interactions common in fraud patterns
- Coefficients directly indicate feature direction and (roughly) magnitude of effect
