# 07 — Transaction Anatomy

This folder breaks down exactly what data fields make up a transaction record — the raw material every downstream feature and model is built from.

## Key Points

- These fields are the columns you'll see in real transaction datasets
- Not all fields are equally predictive — some need transformation to become useful features
