# Transaction History

The sequence of a customer's past transactions, which forms the baseline against which the current transaction's normalcy is judged.

## Key Points

- Nearly all powerful fraud features are derived by comparing the current transaction to this history
- Requires careful, leakage-free construction — only history *before* the current transaction is valid
