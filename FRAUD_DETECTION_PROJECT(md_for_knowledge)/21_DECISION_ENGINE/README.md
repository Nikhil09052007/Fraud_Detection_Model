# 21 — Decision Engine

The system that takes a risk score and turns it into a concrete action — approve, review, or block — by combining the model with business rules.

## Key Points

- This is where the model's output actually affects real customers and real money
- Always combines ML output with explicit business rules, never the model alone
