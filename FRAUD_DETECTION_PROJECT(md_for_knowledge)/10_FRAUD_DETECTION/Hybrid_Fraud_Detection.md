# Hybrid Fraud Detection

Real production systems combining rules (for known hard-fail cases), ML models (for nuanced scoring), and manual review (for edge cases) into one decision pipeline.

## Key Points

- Rules handle certainties (sanctioned countries, stolen card lists)
- ML handles probabilistic, nuanced risk
- Manual review handles the ambiguous middle ground the model itself flags as uncertain
