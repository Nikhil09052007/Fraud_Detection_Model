# Anomaly Detection

Unsupervised techniques that flag data points statistically different from the norm, without needing labeled fraud examples.

## Key Points

- Useful when labeled fraud data is scarce or for detecting entirely new fraud patterns
- Methods: isolation forests, clustering, autoencoders, statistical outlier tests
- Higher false positive rates than supervised methods trained on real labels
