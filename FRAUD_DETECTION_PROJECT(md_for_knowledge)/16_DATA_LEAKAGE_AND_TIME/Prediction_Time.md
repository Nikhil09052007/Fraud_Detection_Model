# Prediction Time

The exact timestamp at which the model makes its prediction — synonymous with the Prediction Point from Section 13 — the hard boundary for what data is valid to use.

## Key Points

- Every feature's underlying data must be timestamped before or at this moment
- Should be tracked explicitly, not assumed, in any feature engineering pipeline
