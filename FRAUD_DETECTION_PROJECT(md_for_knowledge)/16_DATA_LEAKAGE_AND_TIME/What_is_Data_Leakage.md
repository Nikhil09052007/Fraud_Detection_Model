# What is Data Leakage?

When information that would not actually be available at prediction time is accidentally included in training features, causing the model to learn from 'answers' it won't have access to in production.

## Key Points

- Produces deceptively high offline performance that collapses in production
- One of the single most common causes of fraud model failure in the real world
