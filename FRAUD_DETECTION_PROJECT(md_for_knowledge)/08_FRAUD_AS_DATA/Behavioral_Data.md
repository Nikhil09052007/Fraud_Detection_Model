# Behavioral Data

Data capturing patterns of behavior over time — login frequency, typical spend, typical merchants, typical times — rather than single events.

## Key Points

- The foundation of anomaly-based detection: comparing a new event to an established pattern
- Requires historical aggregation, often computed as rolling features
