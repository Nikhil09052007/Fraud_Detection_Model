# Labels

The historical ground-truth values of the target variable across the training dataset — the 'answer key' the model learns from.

## Key Points

- Distinct from the target variable definition — labels are the actual realized values in your dataset
- Label delay and noise (see Section 08) are central challenges in fraud specifically
