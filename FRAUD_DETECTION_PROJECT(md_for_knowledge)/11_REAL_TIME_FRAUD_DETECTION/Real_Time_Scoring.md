# Real-Time Scoring

Passing the generated features through the trained model to produce a risk score, within the live transaction flow.

## Key Points

- Model inference must be fast — typically single-digit to low double-digit milliseconds
- Model complexity is often traded off against latency requirements
