# What is Feature Engineering?

The process of transforming raw data into input variables (features) that better expose the underlying patterns a model needs to learn to distinguish fraud from legitimate activity.

## Key Points

- Encodes domain knowledge into a form the model can use directly
- Often the difference between a mediocre and an excellent fraud model
