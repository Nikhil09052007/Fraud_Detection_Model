# Behavioral Risk Signals

Higher-level, often composite features combining multiple raw signals into a single risk indicator — e.g. a 'suspicious session score' combining device, velocity, and location anomalies.

## Key Points

- Often the most engineered and iterated-upon feature category
- Can incorporate outputs from separate sub-models (e.g. a device-risk model) as inputs to the main fraud model
