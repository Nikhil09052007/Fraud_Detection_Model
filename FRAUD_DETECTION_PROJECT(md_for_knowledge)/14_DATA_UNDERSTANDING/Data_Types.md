# Data Types

The technical type of each column — numeric, categorical, boolean, datetime, text — which determines how it can be processed and modeled.

## Key Points

- Wrong type inference (e.g. treating a categorical ID as numeric) silently corrupts models
- Datetime columns need explicit parsing and timezone handling
