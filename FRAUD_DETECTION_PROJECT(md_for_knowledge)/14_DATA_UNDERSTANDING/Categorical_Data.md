# Categorical Data

Columns representing discrete categories — merchant category, payment method, country — that need special encoding for most ML models.

## Key Points

- High-cardinality categoricals (e.g. merchant ID) need careful encoding to avoid overfitting
- Common encodings: one-hot, target/frequency encoding, embeddings
